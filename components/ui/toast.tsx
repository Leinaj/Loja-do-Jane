'use client';

// Placeholder: não depende do carrinho e não quebra o build.
// Mantém a assinatura para quem estiver importando <ToastBridge />.
export default function ToastBridge() {
  return null; // sem toasts por enquanto
}